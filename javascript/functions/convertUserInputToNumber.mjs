// javascript\functions\convertUserInputToNumber.mjs

function convertUserInputToNumber(userInput) {
  const convertedUserInput = parseInt(userInput, 10);

  return convertedUserInput;
}

export default convertUserInputToNumber;
